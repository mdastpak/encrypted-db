package routers

import (
	"encrypted-db/services"

	"github.com/gin-gonic/gin"
)

func UserRoutes(router *gin.RouterGroup, services *services.Services) {
	// userGroup := router.Group("/user")
	// {
	// 	// userGroup.GET("/profile", user.ProfileHandler(services))
	// 	// userGroup.PUT("/update", user.UpdateHandler(services))
	// }
}
