server:
  ip: "127.0.0.1"
  port: "8080"

postgres:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "afnm640"
  dbname: "postgres"

redis:
  host: "localhost"
  port: "6379"
  password: ""
  db: 0

rabbitmq:
  username: "user"          # Username for RabbitMQ
  password: "pass"          # Password for RabbitMQ
  host: "localhost"         # Host address for RabbitMQ
  port: "5672"              # Port for RabbitMQ
  exchanges:
    currency: "currency_exchange"       # Exchange for currency updates
    user: "user_exchange"               # Exchange for user-related updates
    notifications: "notification_exchange" # Exchange for notifications
